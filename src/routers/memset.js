export default function (core, router, hook) {
  router

  /**
   * Returns stats on all memset
   */
    .get('/', (ctx) => {
      // TODO Totals of each queue
      ctx.body = {

      };
    })

}
